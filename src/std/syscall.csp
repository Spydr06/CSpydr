import "types.csp";

namespace std {
    type Syscall: enum {
        EXIT = 60
    };

    namespace syscall {
        fn exit(status: i32)
        {
            asm "mov " status        ", %rdi;"
                "mov " Syscall::EXIT ", %rax;"
                "syscall";
        }
    }
}
# an implementation of a vector data type

import "types.csp";

# --- the vec_data struct and vec typedef ---
type vec_s: struct {
    data: &&void,
    alloc: usize,
    len: usize
};
type vec: &vec_s;

# TODO: add typeof/sizeof expressions
import "stdio";

type Integer: i32;

type Test: struct {
    x: *[]i32,
    y: *[]i32
};

type State: enum {
    STATE_OK,
    STATE_ERR
};

let x: str = "Hello World";

fn main(argc: i32, argv: str) {
    let a: Integer;
    a += 3;

    let b: *Integer = &3;
    let c: Integer = [(2 + a--) * (*b.s)++, 3, 67];
    let d: Integer = c[4];

    if -b == !a {
        
    }

    test(2, 3);
}

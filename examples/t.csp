import "s.csp";

type Integer: i32;

type Test: struct {
    x: *[]i32,
    y: *[]i32
};

type State: enum {
    STATE_OK,
    STATE_ERR
};

let message: str = "Hello World";
let longNumber: u64 = 1_000_000_000;

fn main(argc: i32, argv: str): i32 {
    let test: Test = {x: [4, 2], y: [3, 2]};

    if test.x[0] > test.y[1] {
        puts(message);
    }

    ret STATE_OK;
}

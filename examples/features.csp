import "std.csp";

type T: struct {
    a: i64,
    b: i64,
    c: i64
};

fn main(): i32 {
    using std;
    
    io::printf("%i\n", 
        T :: {1, 2, 6} |> _.c: i32 |> _ `add` _
    );

    <- 0;
}

fn add(a: i32, b: i32): i32 {
    <- a + b;
}
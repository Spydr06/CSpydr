import "std.csp";

fn main(): i32 
{
    using std::io;

    do {
        puts("1"); # should be executed
    } unless false;

    do {
        puts("2"); # should not be executed
    } unless true;

    do {
        puts("3"); # should be executed once 
    } while false;

    <- 0;
}
import "std.csp";

type T: struct {
    a: i32,
    b: i32,
    c: i32
};

fn main(): i32 {
    using std;

    T :: {1, 2, 3} 
    |> $.a + $.b + $.c
    |> io::printf("%i\n", $);

    [1, 2, 3, 4, 5]
    |> io::printf("%i %i\n", $[^1], $[^3]);

    <- 0;
}
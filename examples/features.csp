import "std.csp";

fn main(): i32 
{
    using std;

    let x = [11, 22, 33, 44, 55, 66, 77, 88, 99];
    test(x);
    <- 0;
}

fn test(array: i32[]) {
    std::io::printf("%i %i\n", len array, array[0]);
}
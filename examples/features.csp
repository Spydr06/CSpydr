import "io.csp";
import "mem.csp";

fn main(argc: i32, argv: &&char): i32 {
    using std;

    with x = mem::alloc(1024) {
        io::printf("%p\n", x);
    }
    else {
        io::printf("pointer allocation failed");
    }

    <- 0;
}
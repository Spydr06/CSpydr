import "std.csp";

fn main(): i32 
{
    print_array([1, 2, 3, 4, 5]);    
    print_array([11, 22, 33]);

    <- 0;
}

fn print_array(array: i32[])
{
    using std::io;

    putc('[');
    for let i = 0; i < len array; i++; {
        printf("%i, ", array[i]);
    }
    puts("]");
}
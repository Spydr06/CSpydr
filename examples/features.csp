import "io.csp";

macro t(a, b) {
    a * b
}

macro t(a) {
    a
}

fn main(): i32 {    

    let buf: char[3];
    std::io::write(stdout!, std::c_str::from_int(t!(5), buf, 10));
    std::io::write(stdout!, std::c_str::from_int(t!(5, 2), buf, 10));

    <- 0;
}
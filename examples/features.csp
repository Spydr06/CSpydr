import "std.csp";

type T: struct {
    a: i32,
    b: i32,
    c: i32
};

fn main(): i32 {
    using std;

    with s = str!{} {
        concat!(s, "Hello, World!");
        io::printf("%s\n", s);
    }

    T :: {1, 2, 3} 
    |> $.a + $.b + $.c 
    |> io::printf("%i\n", $);

    <- 0;
}
import "std.csp";

fn main(): i32 
{
    using std;

    assert!(true, "this should not trigger");
    assert_false!(false, "this should not trigger");
    assert!(false, "this should trigger an assertion");

    <- 0;
}
import "libc/stdio.csp";
import "io.csp";
import "process.csp";


fn main(): i32 {
    let uts: std::UTSName;
    std::syscall::uname(&uts);

    printf("struct utsname {\n  sysname = %s\n  nodename = %s\n  release = %s\n  version = %s\n  machine = %s\n  domainname = %s\n}\n", uts.sysname, uts.nodename, uts.release, uts.version, uts.machine, uts.domainname);

    <- 0;
}
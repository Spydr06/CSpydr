import "std.csp";

fn main(): i32 
{
    using std::io;
    let i = 0;
    defer {
        i++;
        printf("y %i\n", i);
    }
    <- || i32 => {
        defer {
            i++;
            printf("x %i\n", i);
        }
        ret i;
    }();
}
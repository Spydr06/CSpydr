import "libc/stdio.csp";
import "vec.csp";

fn main(argc: i32, argv: &&char): i32
{
    for let i = 0; i < argc; i++;
        printf("%s\n", argv[i]);

    let v = vec![i32];
    vec_add!(v, 1);
    vec_add!(v, 2);
    vec_add!(v, 3);
    <- 0;
}
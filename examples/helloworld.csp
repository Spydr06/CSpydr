import "libc/stdio.csp";
import "libc/stdlib.csp";

type Foo: struct {
    a: i32,
    b: &char
};

fn main(): i32
{
    printf("Hello, World!\n");

    let foo = Foo :: {1, "Hello!"};

    printf("%s %d\n", foo.b, foo.a);

    <- 0;
}

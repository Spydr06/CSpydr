import "libc/stdio.csp";
import "libc/stdlib.csp";

namespace foo {
    fn bar() {
        printf("%ld\n", alignof i32);
    }
}

fn main(argc: i32, argv: &&char): i32 
{
    printf("%s:%d: Hello, World!\n", __file__!, __line__!);
    foo::bar();
    <- 0;
}

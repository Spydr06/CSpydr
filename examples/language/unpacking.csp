import "io.csp";

fn main(): i32 {
    using std;
    
    let arr = [1, 2, 3];
    let arr2 = [...arr, 4, 5];

    io::printf("%i, %i, %i\n", ...arr);
    io::printf("%i, %i, %i, %i, %i\n", arr2...);

    <- 0;
}
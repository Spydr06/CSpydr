namespace x {
    fn puts(str: &char) {
        puts(str);
    }
}

type data: struct {
    msg: &char
};

let program_data: &data;

fn main(argc: i32, argv: &&char): i32 {
    program_data = &({"Hello World"}: data);

    x::puts(program_data.msg);

    <- 0;
}
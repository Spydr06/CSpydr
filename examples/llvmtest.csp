import "file.csp";

fn main(argc: i32, argv: &&char): i32
{
    let my_file = file!("/home/spydr/.bashrc");
    write!(my_file, "/home/spydr/test");

    printf("%s\n", my_file.buff);

    std::file::free(my_file);

    <- 0;
}
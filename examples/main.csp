# a basic test program used for developing this programming language
type foo_struct: struct {
    foo: i32,
    bar: u64
};

fn main(x: i32): i32 {
    let y: i32;
    
    call(y, x);

    y[4] = 5;

    let z: foo_struct = {-235, 0xffffffc};
    let e: enum {
        E_OK,
        E_ERR,
        E_NIL
    } = E_OK;
    

    loop x {

    }

    match x {
        3 => {

        }

        _ => {

        }
    }    
}

let global: i32 = 45;

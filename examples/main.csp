# a basic test program used for developing this programming language

let test: bool = true;
let testArr: [i32] = [4, 4, 4];

fn main(argc: i32, argv: [str]): i32 => {
    let foo: u64 = 5;
    let arr: [[i32: 2]: 2] = nil;
    arr = [[1, 3], test - 1];

    <- 0
}

fn print_(msg: str) => printf("%s\n", msg);
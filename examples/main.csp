# a basic test program used for developing this programming language 

import "string.csp";

fn main(argc: i32, argv: **char): i32 {
    let string: str = init_str("Hello World");
    str_append(string, '!');

    printf("str: %s\n", str_lit(string));

    free_str(string);
    <- 0;
}
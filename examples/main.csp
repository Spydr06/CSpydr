# a basic test program used for developing this programming language
fn main(x: i32): i32 {
    let y: i32;
    
    call(y, x);

    y[4] = 5;

    let z: struct {
        foo: i32,
        bar: u64
    };

    let e: enum {
        E_OK,
        E_ERR,
        E_NIL
    } = E_OK;
    

    loop x {

    }

    match x {
        3 => {

        }

        _ => {

        }
    }    
}
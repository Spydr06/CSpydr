# a basic test program used for developing this programming language

let test: bool = true;

fn main(argc: i32, argv: vec<str>): i32 = {
    let foo: u64 = 5;

    <- 0
}

fn print(msg: str) = printf("%s\n", msg);
# fibonacci.csp
import "io.csp";

fn fib(n: i32): i32 
    ret if n < 2 => 1 
        else fib(n - 1) + fib(n - 2);

fn main(): i32 {
    let n = 10;
    std::io::printf("fib(%i) = %i\n", n, fib(n));

    <- 0;
}